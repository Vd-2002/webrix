/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["[IP_ADDRESS]", "[IP_ADDRESS]", "[IP_ADDRESS]", "[IP_ADDRESS]"],
};

export default nextConfig;
